<div class="credentials" aria-label="Advisor credentials">
  <% advisor.professional.credentials.forEach(item => { %>
    <div class="credential">
      <strong><%= item.value %></strong>
      <span><%= item.label %></span>
    </div>
  <% }) %>
</div>
