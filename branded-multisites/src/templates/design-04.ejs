<!doctype html>
<html lang="en">
<%- include('partials/head', data) %>
<body class="design-04">
<%- include('partials/nav', data) %>
<main>
  <section class="hero hero-social">
    <div class="container social-hero-grid">
      <div class="social-copy">
        <p class="eyebrow">Nice to meet you</p>
        <h1><%= advisor.identity.name %></h1>
        <p class="hero-positioning"><%= advisor.identity.positioning %></p>
        <% if (advisor.trust.trustpilot.enabled && advisor.trust.trustpilot.reviewCount) { %>
          <a class="tp-rating-badge" href="<%= advisor.trust.trustpilot.profileUrl %>" target="_blank" rel="noopener noreferrer" aria-label="View Trustpilot profile">
            <p class="tp-review-count">Based on <%= Number(advisor.trust.trustpilot.reviewCount).toLocaleString() %> reviews</p>
            <span class="tp-stars-row" aria-hidden="true">
              <% for (let i = 1; i <= 5; i++) { const fill = Math.max(0, Math.min(1, Number(advisor.trust.trustpilot.rating) - (i - 1))); %>
                <span class="tp-star-cell">
                  <span class="tp-star-fill" style="width:<%= (fill * 100).toFixed(0) %>%"></span>
                  <span class="tp-star-icon"></span>
                </span>
              <% } %>
            </span>
            <span class="tp-logo" aria-hidden="true">
              <span class="tp-logo-star"></span>
              <span class="tp-logo-text">Trustpilot</span>
            </span>
          </a>
        <% } else { %>
          <div class="trust-summary trust-summary--fallback">
            <strong>Professional guidance</strong>
            <span>Clear, personal, no-pressure conversations</span>
          </div>
        <% } %>
        <div class="mini-proof"><span><%= advisor.identity.location %></span></div>
      </div>
      <img src="<%= images.headshot.src %>" alt="<%= images.headshot.alt %>" width="<%= images.headshot.width %>" height="<%= images.headshot.height %>" fetchpriority="high">
    </div>
  </section>

  <%- include('partials/reviews', data) %>

  <% if (advisor.trust.trustpilot.enabled && advisor.trust.trustpilot.reviewCount) { %>
    <div class="container">
      <div class="trust-bar">
        <div class="trust-bar-inner">
          <a href="<%= advisor.trust.trustpilot.profileUrl %>" target="_blank" rel="noopener noreferrer" class="trust-bar-link">
            <div class="trust-bar-block">
              <div class="trust-bar-stars" aria-hidden="true">
                <% for (let i = 0; i < 5; i++) { %><svg class="trust-bar-star" viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"></path></svg><% } %>
              </div>
              <span class="trust-bar-score"><%= advisor.trust.trustpilot.rating %> / 5 on Trustpilot</span>
              <span class="trust-bar-count"><%= Number(advisor.trust.trustpilot.reviewCount).toLocaleString() %>+ verified reviews</span>
            </div>
          </a>
          <div class="trust-secondary-line">
            <% advisor.professional.credentials.slice(0, 3).forEach(credential => { %>
              <span>
                <svg viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"></path></svg>
                <%= credential.value %> <%= credential.label %>
              </span>
            <% }) %>
          </div>
        </div>
      </div>
    </div>
  <% } %>

  <section class="section">
    <div class="container split-proof" id="credentials">
      <div><p class="eyebrow">Credentials</p><h2>A quick snapshot of my background.</h2></div>
      <%- include('partials/credentials', data) %>
    </div>
  </section>

  <section class="section story-section" id="story">
    <div class="container narrow">
      <h2>What shaped my approach</h2>
      <% advisor.professional.story.paragraphs.forEach((paragraph, i) => { %><p<%= i === 0 ? ' class="lead-copy"' : '' %>><%= paragraph %></p><% }) %>
      <figure class="story-signature">
        <blockquote>“<%= advisor.professional.story.quote %>”</blockquote>
        <figcaption>— <span class="signature-name"><%= advisor.identity.firstName %></span></figcaption>
      </figure>
    </div>
  </section>

  <section class="section lifestyle-section" id="lifestyle">
    <div class="container">
      <p class="eyebrow">Beyond the office</p>
      <h2><%= advisor.personal.moreAboutMe.title %></h2>
      <div class="lifestyle-grid">
        <% const [photo] = advisor.personal.lifestylePhotos; %><img src="<%= photo.src %>" alt="<%= photo.alt %>" width="<%= photo.width %>" height="<%= photo.height %>" loading="lazy">
        <div class="lifestyle-copy"><% advisor.personal.moreAboutMe.paragraphs.forEach(paragraph => { %><p><%= paragraph %></p><% }) %></div>
      </div>
    </div>
  </section>

  <%- include('partials/faq', data).trim() %>
  <%- include('partials/contact', data) %>
</main>
<%- include('partials/footer', data) %>
<%- include('partials/schema', data) %>
</body></html>
